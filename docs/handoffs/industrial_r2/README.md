# SABLE HARBOR Full-State Codex Handoff Package — R2

Upload the entire ZIP to the Codex chat and instruct Codex to read `SABLE_HARBOR_ARU_RED_WASH_PALE_SUN_CODEX_HANDOFF_FULL_STATE.md` completely before touching the repository.

This R2 package supersedes the earlier handoff. It explicitly authorizes Codex to implement the full scope, commit it, push it, use a pull request if useful or required, resolve CI and merge conflicts, merge the validated result into `main`, and push `main`. A PR is delivery plumbing, not a user-approval stop.

The package itself was assembled outside the repository and therefore did not itself modify GitHub. The authority it gives Codex is stated in Section 0 and in `DECISION_LEDGER_FULL.json`. Earlier no-write language preserved inside historical reconciliation appendices is superseded.

The package contains the full execution prompt, machine-readable decision ledger, exact image assets with SHA-256 manifest, and both earlier reconciliation reports as historical appendices.
