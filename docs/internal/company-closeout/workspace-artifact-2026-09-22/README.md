# Non-authoritative workspace output — bounded issue #34 implementation

The owner instructed completion of the company edition. This implementation fills the explicit issue #34 output-marking criterion without creating a model service or institutional-authority route. Repository acceptance remains pending; this receipt is actual local software evidence, not an accepted canon claim.

[EXAMPLE.json](EXAMPLE.json) is an actual exported working excerpt from the accepted public Daedalus doctrine at commit `7ce2ed5c0751ca596a5540bd8afbc0072ef99016`. [RECEIPT.json](RECEIPT.json) records exact source/artifact hashes and the private-write/read operation. The synthetic exercise owner is not a new employee. Only already public source content and fictional exercise metadata are published.

`enterprise.ccf.company_closeout.workspace_artifact.compose` checks current source disclosure rights and exact source bytes, then emits one fixed wrapper: SYNTHETIC_NON_AUTHORITATIVE_WORKSPACE_ARTIFACT, explicit no-institutional-authority/no-promotion flags, source identity/version/hash/times, transformation and excerpt digest. It extracts at most 2,000 Unicode characters, with no model invocation or inference. Source instructions remain quoted data; they cannot change the wrapper or authorize promotion.

`publish` actually creates a new private JSON artifact exclusively; no overwrite or raw unmarked export option exists. `read` checks the retained artifact hash, required markings, owner/tenant/purpose, current source rights and current source version/hash before returning the wrapper. It refuses another user's memory, revoked access, stale derivatives, altered files and authoritative-write/promotion requests. This is a trusted authenticated local operator interface: callers cannot grant themselves authority by constructing a subject dictionary. Runtime authentication, latest revocation checkpoint and filesystem custody remain the existing adapter's responsibility.

The public example is a **working excerpt**, not an institutional judgment or a new approved policy. Marking a copy does not erase the original document's accepted status. No canonical source, CompanyStore original, Canon/Semaphore/EIB entry, employee population or financial record is changed. Users may deliberately publish this public example through repository review; that is separate from any automatic AI promotion.

Verification: `python -m pytest -q tests/ccf/test_workspace_artifact.py tests/ccf/test_information_policy.py` passed 68 tests. The eight artifact tests exercise actual files, fixed marking, complete source provenance, prompt text as inert data, rejected promotion routes, personal isolation, revocation, stale versions, malformed/tampered exports, duplicate publication and future/restricted-source denial. These are deterministic output/policy tests, **not model-specific inference or prompt-injection effectiveness evaluation**, secure erasure, or full live Daedalus deployment.
