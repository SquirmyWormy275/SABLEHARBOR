# Final company completion evidence — September 22, 2026

**State:** Implementation awaiting its own reviewed merge and versioned release. Edition 1.1.0 remains unchanged. The owner's continuation instruction was “OK. Finish everyhing”; this authorizes completing the remaining ordinary synthetic records and software work, not inventing real external acts.

## Ownership and runtime scope

The active audit portal remains in `SABLEHARBOR-audit-suite` on `build/audit-training-suite`. Its latest observed committed revision was `6f095bef`; this work does not edit its worktree, restart servers or migrate private stores. Company policy and restore adapters are owned here under `enterprise/ccf/company_closeout/information_policy.py` and `tools/company_closeout/working_copy_*`. The exercised native disposal implementation remains explicitly pinned to `5344bf76413ffe2cd378b957dbb5f7d56d04ea19`, whose API was already used by edition 1.1.0. Later UI/checkpoint work is not claimed through that pin.

Accepted immutable source/archive history stays in the existing CompanyStore. Separately declared working copies have explicit lifecycle decisions. An older file backup is restored only against the *current*, independently retained native disposal journal and grant state. Missing/stale checkpoints, pending operations, changed populations/bytes, unattributed missing records and concurrent state changes fail closed. Disposed working copies are suppressed before publication. Held bytes remain private and undisclosed. A fresh restore cannot reinstate a revoked grant.

`WORKING_COPY_RESTORE_RECEIPT.json` records an actual isolated software exercise on copied public edition 1.1.0 records: three original working copies, two later deletions, a subsequently held retained copy, native grant revocation and 27 positive/negative results. Backup bytes were captured before disposal, not reconstructed from a later journal. The current checkpoint includes the native grant population hash. Read, search/snippet, citation, count, graph, tool/answer context, export and personal-memory requests are denied before rejected bytes enter caller context; restricted-existence notices remain separate in company policy. Limited metadata/count grants do not yield full source bytes. No vector index is present in this declared working-copy environment; vector requests cannot disclose payload.

This is a trusted local operator boundary. The adapter's policy context must come from authenticated callers; it is not a new Internet service or authorization for arbitrary callers to select identities. Actual lifecycle/restore tests, deterministic hostile-source tests and model-specific inference evaluation remain different claims. No model-specific evaluation, secure erasure, all thirteen external estate gates or live portal deployment is asserted. The new bounded software evidence addresses the company's reproducible inspection contract; external facilities and third-party deployments remain outside that contract.

## Repository administration

`BRANCH_RETIREMENT.json` records 35 exact remote refs retired after fresh tip, ancestry, unique-object, local-worktree and open-PR checks. Every retired object remains reachable from accepted main; a single atomic push used exact SHA leases. Sixty-seven remote refs remain explicitly retained for active work, unique objects, historical preservation or unavailable ownership proof. Local worktrees were not deleted. Main protection remains in force; automatic branch deletion stays disabled to preserve active parallel work. The always-required Source integrity job now also executes organization/brand/governance validation, company accounting/disclosure regressions, a fresh financial build, independent financial reperformance and export import. This avoids path-filtered domain checks being absent from a required-status gate. Its successful hosted result is required before merging this successor. The CCF workflow's existing checkout v4, setup-uv v6 and upload-artifact v4 actions are now pinned to their September 22 resolved commits, without changing their major versions or workload.

## Exact-image and geographic boundaries

`IMAGE_RECOVERY.json` records a broader exact-hash search over 65,279 unique local raster files: zero matches and zero read errors. The approved HQ original remains unavailable. A replacement image cannot satisfy the approved hash; no recreation is labeled recovered identity.

The [current geographic criterion matrix](../GEOGRAPHIC_108_CURRENT_DISPOSITION_2026-09-22.md) replaces the stale 8,961-carrier backlog claim with the accepted complete 78,145-carrier disposition. It preserves the separate rail-engineering remainder, historical precision limits and later-source review boundary. A complete financial/operating company edition does not assert an unperformed engineering survey.

## Reproduction

Use the supported `uv sync --frozen --all-extras` environment and the accepted source revision recorded in the final release. Run the company policy and restore tests. For the native exercise, use a clean isolated checkout of portal `5344bf76` and the downloaded public edition ZIP:

```sh
python -m tools.company_closeout.working_copy_rehearsal \
  --portal /path/to/pinned-portal \
  --package /path/to/sable-harbor-company-edition-v1.1.0.zip \
  --package-sha256 774dadd50fa57be0d5b342b6470b933e83f8b5e705082b82236f64e98f25dff4 \
  --output /new/private/isolated-directory
```

The output parent must be private; the destination must not exist. Never publish its private SQLite stores or working payloads. Retain only public receipts and selected synthetic evidence. A final release receipt supersedes pending implementation labels only after actual source acceptance, validation, packaging and retrieval.

The exact pinned runtime is delivered through the [controlled dependency](RUNTIME_DEPENDENCY.json). Its private source archive closes the previously local-only dependency gap without publishing private historical development material.
