SELECT source_type,COUNT(DISTINCT source_id) AS source_records,COUNT(*) AS journal_entries FROM journal_entry WHERE generation_run_id IN (:actual_run_id,:generation_run_id) GROUP BY source_type ORDER BY source_type;
