SELECT metric_code,entity_code,COUNT(DISTINCT scenario_code) AS scenario_count,MIN(amount) AS low,MAX(amount) AS high FROM scenario_value WHERE generation_run_id=:generation_run_id GROUP BY metric_code,entity_code ORDER BY metric_code,entity_code;
