# Blackridge Excel Workbook Guide

`BLACKRIDGE_MASTER_TRACKER_v0.1.0.xlsx` is generated exclusively from public SQL. Workbook metadata
records dataset/schema versions, seed, and database hash. Financial and Phase 4 surfaces contain
query-derived values; high-volume surfaces state that complete records belong in SQL/CSV. The
workbook is an interface, never a second source of truth.

